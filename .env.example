# Origin бэкенда (без /api/v1): для fetch к `/api/v1/...` и для URL `/uploads/...`
# VITE_API_URL=http://localhost:8000

# Публичный URL фронтенда — canonical и мета-теги (без слэша в конце)
VITE_SITE_URL=https://dorogadomoy.by
