/**
 * Layout class strings — синхронизированы с layout.css.
 */

/** Основной контейнер приложения (max 1200px). */
export const pageContainer = 'page-container';

/** Средний контейнер (1024px) — профиль, избранное, гайды. */
export const pageContainerMedium = 'page-container-medium';

/** Узкий контейнер (896px) — настройки, terms, формы. */
export const pageContainerNarrow = 'page-container-narrow';

/** Readable (768px) — FAQ, длинный текст. */
export const pageContainerReadable = 'page-container-readable';

/** Вертикальные отступы секции приложения. */
export const pageSectionY = 'page-section-y';

/** Лендинг: секция / hero. */
export const landingSectionY = 'landing-section-y';
export const landingHeroY = 'landing-hero-y';

/** Стек блоков с gap 24px. */
export const pageStack = 'page-stack';

/** Отступ под заголовок страницы. */
export const pageHeaderBlock = 'page-header-block';

/** Заголовок секции лендинга по центру. */
export const landingSectionHeader = 'text-center page-header-block';

/** @deprecated alias — используйте pageContainer */
export const landingContainerWide = pageContainer;

/** @deprecated alias — используйте pageContainer */
export const landingContainerNarrow = pageContainer;

/** @deprecated alias — используйте pageContainerReadable */
export const landingContainerReadable = pageContainerReadable;
