/**
 * Типографическая система — class strings (синхронизированы с typography.css).
 * h1: 36px / 600 / tight — один на страницу
 * h2: 30px / 600 — секции
 * h3: 20px / 500 — подсекции
 * h4: 18px / 500 — карточки
 */

/** Hero / заголовок страницы (responsive: 30px → 36px). */
export const typoH1 =
  'typo-h1';

/** Заголовок секции. */
export const typoH2 =
  'typo-h2';

/** Подсекция, блок виджета. */
export const typoH3 =
  'typo-h3';

/** Заголовок карточки. */
export const typoH4 =
  'typo-h4';

/** Основной body-текст. */
export const typoBody =
  'typo-body';

/** Подписи, метаданные. */
export const typoCaption =
  'typo-caption';

/** Lead / intro под заголовком. */
export const typoLead =
  'typo-lead';

/** Ссылка внутри текста (статьи, описания). */
export const typoLink =
  'typo-link';

/** Декоративный display (404, empty states). */
export const typoDisplay =
  'typo-display';

/** Компактный заголовок toolbar (match swipe). */
export const typoH1Compact =
  'typo-h1-compact';

/** h1 на тёмном hero (ShelterDetail). */
export const typoH1OnDark =
  'typo-h1 text-white';
