/** Yandex.Metrika counter id */
export const YM_ID = 107705476;

/** Session storage keys for SPA route history */
export const PREV_ROUTE_KEY = 'dd_previous_path';
export const CURRENT_ROUTE_KEY = 'dd_current_path';

/** PWA / brand — см. `src/shared/styles/tokens.ts` */
import { tokens } from '@/shared/styles/tokens';

export { tokens };
export const BRAND_PRIMARY = tokens.colors.primary;
